This project provides a tool to parse geographic
names or codes. It handles caching geographic
names, codes, and primary keys from the database,
and uses a "fuzzy" matching algorithm for matching names.
