         .FEATURE LABELS_WITHOUT_COLONS

         .EXPORT M6502VEC

         ;MONITOR
         .IMPORT RESET
         ;DEBUG
         .IF VERSION < 2
         .IMPORT IRQ
         .ELSE
         .IMPORT RESET2
         .ENDIF

         .INCLUDE "SYMBOLS.A65"

M6502VEC .ADDR NMI        ;NMI VECTOR
         .IF VERSION < 2
         .ADDR RESET      ;RESET VECTOR
         .ADDR IRQ        ;IRQ VECTOR
         .ELSE
         .ADDR RESET2
         .ADDR RESET
         .ENDIF
